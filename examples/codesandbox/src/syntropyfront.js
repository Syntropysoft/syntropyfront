// Import SyntropyFront from the .cjs file and re-export as ES module
import syntropyModule from './syntropyfront.cjs';

// Export the default instance (that's all we have now)
export default syntropyModule; 